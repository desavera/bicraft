# B2W authenticated docker run
# sudo dockerd --insecure-registry registry.atlas.b2w:5000
#
# RUN SUPERSET
# docker run --detach --name superset [options] amancevice/superset
# INIT DATABASE
# docker exec -it superset superset-init
sudo docker run --detach --name superset --add-host=lvnn-priv.b2w:10.13.9.20 -p 8088:8080 --expose 8088 --dns=10.234.80.47 --dns-search=la.ad.b2w --volume `pwd`:/home/superset/.superset amancevice/superset
